# Log técnico sanitizado

- Executável WhisperX validado: `/home/vinicius-alves/.venvs/whisperx/bin/whisperx`.
- Processamento local executado pela API WhisperX do mesmo ambiente virtual, em processo filho iniciado com lista de argumentos, usando modelo `large-v3`, idioma `pt`, dispositivo `cpu`, `compute_type` `int8` e `batch_size` `2`.
- Alinhamento executado.
- Diarização executada com sucesso; identificados três locutores anônimos: `SPEAKER_00`, `SPEAKER_01` e `SPEAKER_02`.
- Arquivos brutos gerados: JSON, SRT e TXT.
- Nenhum conteúdo completo da transcrição, token ou credencial foi incluído neste log.
