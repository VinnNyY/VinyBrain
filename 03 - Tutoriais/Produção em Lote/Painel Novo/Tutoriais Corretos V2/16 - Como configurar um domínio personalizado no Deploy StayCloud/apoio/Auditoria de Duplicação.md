# Auditoria de Duplicação

Data: 2026-08-03

Tema auditado: `Como configurar um domínio personalizado no Deploy StayCloud`

## Estado da meta

- Meta mensal: `12/12 — concluída`.
- Tutorial anterior: `Como publicar nova versão StayCloud pelo Deploy em 5 passos`.
- URL pública do tutorial anterior: `https://ajuda.staycloud.com.br/docs/publicar-nova-versao-staycloud/`.
- Planilha: atualizada manualmente por Vinicius.
- Produção adicional: `1 tutorial extra publicado e validado`.
- Este tutorial não altera a meta para 13/12.

## Planilha

Planilha auditada: `15gE4co_nzpTjkckIDiVsrOb9jtEYwSE3lQeBjSX9Hso`, aba `Tutoriais`.

Resultado:

- Não há linha dedicada a domínio personalizado no Deploy StayCloud.
- Não há linha dedicada a subdomínio no Deploy StayCloud.
- Há tutoriais relacionados a domínio, nameservers e domínio no StayPanel/cPanel, mas não com o objetivo de conectar um domínio ou subdomínio a uma aplicação publicada pelo novo Deploy.
- O tutorial de nova versão está registrado como `Postado`.

## Central de Ajuda pública

Base auditada: `https://ajuda.staycloud.com.br/`.

Buscas executadas:

- `domínio personalizado Deploy StayCloud`
- `subdomínio Deploy StayCloud`
- `dominio-personalizado-deploy-staycloud`
- `URL personalizada StayCloud`
- `apontar domínio StayCloud`

Resultado:

- Foram encontrados artigos gerais sobre hospedagem, SSL, nameservers, subdomínio/cPanel e publicação de nova versão.
- O artigo publicado de nova versão cita domínio personalizado como assunto fora do escopo.
- Não foi encontrado tutorial publicado com o fluxo específico `Deploy > Cloud > Domínios > Adicionar domínio`.

## Obsidian

Buscas locais executadas:

- `domínio personalizado Deploy`
- `domínio no Deploy`
- `conectar domínio à aplicação`
- `adicionar domínio ao Deploy`
- `subdomínio no Deploy`
- `DNS da aplicação`
- `URL personalizada`
- `domínio da aplicação StayCloud`
- `apontar domínio para Deploy`

Resultado:

- A nota `Deploy StayCloud.md` registrava `Domínios` como aba observada e domínio personalizado como dúvida pendente.
- Os tutoriais V2/12, V2/13 e V2/15 citavam domínio personalizado apenas como assunto separado.
- Não havia pacote V2 pronto para domínio personalizado do Deploy.

## Decisão

Tema classificado como novo conteúdo extra, sem duplicação direta.

Não são duplicados:

- compra de domínio;
- alteração de nameservers;
- domínio adicional no cPanel;
- Zone Editor;
- DNS de hospedagem;
- domínio principal de serviço;
- redirecionamento.

Escopo aprovado: ligação entre domínio/subdomínio próprio e a área `Domínios` do Deploy/Cloud.
