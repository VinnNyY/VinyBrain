# Histórico Codex - 2026-07-03

## Resumo do dia

Nesta sessão, foi registrado e auditado o bootstrap real da integração entre `Notion` e `ClickUp`.

O trabalho ficou dividido em quatro frentes:

1. registrar o estado real da estrutura criada;
2. atualizar decisões e documentação técnica;
3. preparar a próxima fase como testes controlados;
4. consolidar checkpoint e histórico sem expor credenciais.

Mais tarde, a Fase 2 da integração foi iniciada com scripts controlados para testar a criação real de uma página em `Reuniões` e de uma tarefa em `Demandas Internas`, mantendo `--apply` como trava explícita.
Depois disso, a Fase 2 foi concluída com sucesso: a página de teste do Notion e a tarefa de teste do ClickUp foram criadas dentro da estrutura existente.
Na sequência, a Fase 3 foi preparada como fluxo operacional real, ainda com criação controlada e revisão explícita antes de qualquer escrita.
Agora, a Fase 3 foi validada como operação real controlada usando JSON como ponte entre o Viny Flow e as integrações.
A frente de demandas para ClickUp recebeu modo inteligente de captura, modo lista simples e modo lista em lote.

## Linha do tempo

### Etapa 1: registro do bootstrap real

- Foi criado o arquivo `18 - Integrações/API/Estado Atual - Notion e ClickUp.md`.
- A estrutura criada foi registrada de forma nominal, sem token e sem detalhes sensíveis.

### Etapa 2: atualização de decisões

- A decisão de bootstrap real concluído foi adicionada à área de Integrações.
- O `Viny Brain` ficou reafirmado como fonte de memória e raciocínio.
- `Notion` e `ClickUp` ficaram reafirmados como camadas externa e operacional.

### Etapa 3: atualização da documentação técnica

- A documentação da API passou a refletir que o bootstrap real já foi executado.
- O plano de bootstrap passou a apontar os próximos testes controlados.

### Etapa 4: próxima fase documentada

- A próxima etapa foi registrada como `Fase 2 - Testes controlados`.
- O foco passa a ser página isolada no Notion e tarefa isolada no ClickUp.
- A ideia é validar payload, segurança e uso operacional antes de pensar em fluxo maior.

### Etapa 5: início da Fase 2

- Foram criados os scripts `create_test_notion_meeting.py` e `create_test_clickup_task.py`.
- Os scripts localizam a estrutura existente antes de criar qualquer item.
- O comportamento padrão continua sendo dry-run.
- A escrita real só acontece com `--apply`.
- O dry-run do Notion foi validado com localização de `Viny Hub` e `Reuniões`.
- O dry-run do ClickUp foi validado com localização de `Viny Operacional` e `Demandas Internas`.

### Etapa 6: conclusão da Fase 2

- A página de teste do Notion foi criada dentro de `Reuniões`.
- A tarefa de teste do ClickUp foi criada dentro de `Demandas Internas`.
- A integração foi validada para criação real controlada.
- A escrita operacional real ainda não virou rotina.

### Etapa 7: preparação da Fase 3

- A próxima etapa foi registrada como `Fase 3 - Fluxo operacional real`.
- O foco passa a ser transformar saídas manuais em criação real controlada.
- O Notion segue como camada documental.
- O ClickUp segue como camada operacional.
- A Fase 3 agora usa JSON como ponte operacional entre os comandos e as APIs.
- O fluxo de demanda passou a diferenciar captura simples e detalhada.
- O fluxo de demanda passou a diferenciar captura simples, detalhada e em lote.

## Estado atual

- O bootstrap real foi concluído com sucesso.
- A estrutura básica já existe em `Notion` e `ClickUp`.
- Não há criação operacional nova nesta sessão.
- A Fase 2 foi concluída com sucesso com criação real controlada.
- Os tokens continuam fora do vault.
- A Fase 3 foi validada como fluxo operacional real controlado.
- O comando `/processar-demanda` passou a perguntar quando faltar informação crítica e a permitir captura simples.
- O comando `/processar-demanda` passou a perguntar quando faltar informação crítica e a permitir captura em lote.
- Foi criado o script `create_clickup_tasks_batch_from_json.py` para listas rápidas em massa.
- A captura em lote do ClickUp passou a expor lista destino, ID, quantidade, nomes e erro da API quando houver falha.
- Foi criado o script `list_clickup_demandas_internas.py` para verificar o conteúdo atual da lista `Demandas Internas`.
- O payload `lote-tarefas-teste.json` foi aplicado com sucesso e passou a ser tratado como consumido.
- A regra anti-duplicação exige novo JSON para qualquer novo lote.
- A lista `Demandas Internas` foi conferida depois do apply e contém as quatro tarefas do lote mais a tarefa de teste antiga.
- O comando `/processar-demanda` agora diferencia gerar payload de aplicar no ClickUp com autorização explícita.
- O fluxo exige `dry-run` antes de `--apply` e registra o JSON usado para criação real.
- O comando `/processar-demanda` passou a reconhecer o gatilho textual `Adicionar ClickUp` e variações naturais.
- O fluxo passou a usar entrevista rápida quando a entrada vier incompleta.
